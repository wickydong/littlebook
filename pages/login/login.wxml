<!--pages/login/login.wxml-->
<view class="swiper-container">

<block wx:if="{{showloading}}">
    <view class="donut-container">
        <view class="donut"></view>
    </view>
</block>
<block wx:else>
    <button open-type='getUserInfo' bindgetuserinfo="dologin" >需要您的授权</button>
</block>
</view>